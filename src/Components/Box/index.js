import styled from "styled-components";
import {
  color,
  space,
  layout,
  flexbox,
  typography,
  background,
  border,
  position,
  grid,
  shadow,
} from "styled-system";

export const Box = styled.div`
  ${grid}
  ${color}
  ${space}
   ${layout}
  ${flexbox}
  ${typography}
  ${background}
  ${border}
${position}  
${shadow}
`;
